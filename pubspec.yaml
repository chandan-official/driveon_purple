name: driveon_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  google_maps_flutter: ^2.6.1
  geolocator: ^11.0.0

  
 
  flutter_google_places_sdk: ^0.3.1


  cupertino_icons: ^1.0.8
  google_fonts: ^6.3.3
  intl: ^0.20.2
  flutter_dotenv: ^6.0.0
  shared_preferences: ^2.2.3
  http: ^1.2.2
  razorpay_flutter: ^1.3.7
  url_launcher: ^6.3.2
  table_calendar: ^3.2.0
  socket_io_client: ^3.1.4
  share_plus: ^12.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    
    - assets/images/
    - assets/.env
